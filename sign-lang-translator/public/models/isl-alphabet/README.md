# ISL Alphabet Model

Put the converted TensorFlow.js model files in this folder.

Expected files after training:

```text
model.json
group1-shard*.bin
labels.json
```

The React app loads:

```text
/models/isl-alphabet/model.json
```
